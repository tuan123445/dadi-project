/* eslint-disable prettier/prettier */
import {
  createStore
} from "vuex";

const store = createStore({
  state() {
    return {
      user_info: undefined
    }
  },
  mutations: {},
  actions: {},
  modules: {},
});
export default store;